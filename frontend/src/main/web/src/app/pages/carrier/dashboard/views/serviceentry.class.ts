export class ServiceEntry {
  service_id: number;
  service_name: string;

  constructor(service_id: number, service_name: string) {
    this.service_id = service_id;
    this.service_name = service_name;
  }
}
